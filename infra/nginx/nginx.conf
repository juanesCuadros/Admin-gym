# ==============================================================================
# GymOS — Configuración Gateway Nginx (VPS Producción)
# NOTA: Pendiente de definición final por Juan debido a que el servidor VPS
# se comparte con resttodash (puertos 80/443 ya ocupados allí).
# ==============================================================================

# user nginx;
# worker_processes auto;
# error_log /var/log/nginx/error.log warn;
# pid /var/run/nginx.pid;

# events {
#     worker_connections 1024;
# }

# http {
#     include /etc/nginx/mime.types;
#     default_type application/octet-stream;
#     sendfile on;
#     keepalive_timeout 65;
#
#     # Upstreams internos a los servicios de Docker
#     # upstream super_admin_web { server super-admin-vista:80; }
#     # upstream gym_tenant_web { server web:80; }
#
#     # Bloques de server por subdominio (admin.gymos.io vs *.gymos.io)
#     # Se configurarán en coordinación con la infraestructura de resttodash.
# }
